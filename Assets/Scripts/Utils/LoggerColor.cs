using UnityEngine;

namespace Utils
{
    public static class LoggerColor
    {
        public static Color Red = new Color(0.8f, 0.2f, 0.2f);
        public static Color Green = new Color(0.2f, 0.8f, 0.2f);
    }
}